//Q31: No Users:

let users: string[] = [ "Shazia ", "Shafia", "Saima", "Sana", "Admin"];
if (users.length === 0) {
    console.log( "We need to find some users.");
}
//Remove all of the usernames from array:
else {
    users = [];
    console.log( "All user have been removed." + users.length);

}